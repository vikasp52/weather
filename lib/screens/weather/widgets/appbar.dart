import 'package:flutter/material.dart';
import 'package:weather/util/colors.dart';

PreferredSizeWidget weatherAppBar() => AppBar(
      title: const Text('Mumbai'),
      centerTitle: true,
      elevation: 0.0,
      backgroundColor: CustomColor.backgroundColor,
    );
